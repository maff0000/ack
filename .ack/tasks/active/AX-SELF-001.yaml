id: AX-SELF-001
project: ack
type: read
role: scout
model: trinity-deep
project_root: /srv/codex/ACK
base_commit: fd00474f938535bdf46cbeba810e8b99e4ffe0ef
worktree: ""

skills:
  - python
  - redis

objective: Inspect ACK v0.1 against repository evidence and identify exactly one real, small, bounded, testable improvement suitable for the self-hosted Builder proof.

scope:
  - PID.md
  - AXIOM.md
  - README.md
  - .ack
  - tests

must_not:
  - Mutate repository contents.
  - Broaden ACK architecture or scope.
  - Spawn another agent.

acceptance:
  - Run `rg PROJECT_ROOT PID.md`, `find .ack -maxdepth 3 -type f`, and inspect README.md before making claims.
  - Identify exactly one evidence-backed small testable improvement or state that no suitable gap exists.
  - Put the commands actually run in tests.commands.
  - Put the supporting file paths in evidence.
  - Return a concise structured result with factual evidence.
  - Report no changed files and no commit.

dependencies: []
risk: low

authority:
  mutation_allowed: false
  runtime_mutation_allowed: false

status: queued
