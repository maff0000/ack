# Docker

Build environment-neutral images where practical. Supply runtime configuration externally; never bake secrets into images. Define lifecycle and health behaviour, meaningful service names, stdout/stderr logging, and minimal host coupling. Deployment owns bounded log retention.
