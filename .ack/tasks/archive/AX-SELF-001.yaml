id: AX-SELF-001
project: ack
type: read
role: scout
model: trinity-deep
project_root: /srv/codex/ACK
base_commit: fd00474f938535bdf46cbeba810e8b99e4ffe0ef
worktree: ""

skills:
  - python
  - redis

objective: Determine whether README.md documents ACK's structured worker result enforcement and task-derived native Codex sandbox modes; identify exactly one bounded documentation improvement if that explanation is missing or unclear.

scope:
  - README.md
  - .ack/config.example.yaml
  - .ack/templates/result.schema.json
  - .ack/lib/ack/runner.py

must_not:
  - Mutate repository contents.
  - Broaden ACK architecture or scope.
  - Spawn another agent.

acceptance:
  - Run `rg -n 'structured|output-schema|sandbox_mode|read-only|workspace-write' README.md .ack/config.example.yaml .ack/lib/ack/runner.py` before making claims.
  - Identify exactly one evidence-backed documentation improvement or state that no suitable gap exists.
  - Put the commands actually run in tests.commands.
  - Put the supporting file paths in evidence.
  - Return a concise structured result with factual evidence.
  - Report no changed files and no commit.

dependencies: []
risk: low

authority:
  mutation_allowed: false
  runtime_mutation_allowed: false

status: completed
