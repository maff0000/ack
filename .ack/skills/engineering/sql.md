# SQL

Use migrations rather than ad-hoc schema mutation, explicit transactions where appropriate, parameterised queries, externally supplied credentials/configuration, deterministic UTC timestamps, and visible failures. Keep schema changes inside project authority.
