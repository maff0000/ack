id: AX-SELF-002
project: ack
type: write
role: builder
model: trinity-fast
project_root: /srv/codex/ACK
base_commit: 3f671d5f6720b5b345cde6c932702e284c847085
worktree: /srv/codex/ACK/.ack/worktrees/AX-SELF-002

skills: []

objective: Add a concise README explanation of structured worker result enforcement and task-derived native Codex sandbox modes.

# Execute with repository tools before returning a result. Modify README.md only; do not inspect or change the result schema.
# Use this exact order once: edit README.md; run `python3 -m unittest discover -q`;
# run `git add README.md && git commit -m "document worker result and sandbox enforcement"`;
# run `git rev-parse HEAD`; then return every field from the required result shape.

scope:
  - README.md

must_not:
  - Change implementation, contracts, architecture, or unrelated documentation.
  - Add credentials, host-specific secrets, or physical model names.
  - Modify canonical main or merge branches.

acceptance:
  - "README names `.ack/templates/result.schema.json` and explains that structured JSON is YAML-compatible and still validated by ACK."
  - "README explains read tasks use native `read-only` and write tasks use `workspace-write` inside ACK's outer bubblewrap boundary."
  - Documentation remains concise.
  - "`python3 -m unittest discover -q` passes."
  - "Worker commits the change on `ack/AX-SELF-002/worker` and returns that commit."
  - "Use Git commands in the isolated worker repository: stage README.md, create a real commit, and report the exact output of `git rev-parse HEAD`; never invent a commit ID."

dependencies:
  - AX-SELF-001
risk: low

authority:
  mutation_allowed: true
  runtime_mutation_allowed: false

status: queued
