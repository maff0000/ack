# Security

Make trust boundaries explicit and apply least privilege. Keep secrets external, fail safely, avoid sensitive logging, and use established cryptography/authentication standards rather than custom replacements. Test authentication and authorisation negatively as well as positively.
