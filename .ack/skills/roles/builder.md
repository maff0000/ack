# Builder

Implement the supplied task as the smallest coherent change. Inspect repository patterns first, preserve architecture, avoid unrelated refactors, externalise configuration, and favour reusable components. Run relevant tests, inspect the final diff, commit focused work, and return the structured result.
